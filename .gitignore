node_modules/

# Runtime state lives in ~/.config/paseo-k8s, never in the repo. These patterns
# exist so a stray local copy can never be committed by accident.
clusters.json
pointer.json
*.kubeconfig
kubeconfig
.kube/

# Credentials of any shape
*.pem
*.key
*.crt
*.p12
.env
.env.*

*.log
.DS_Store
