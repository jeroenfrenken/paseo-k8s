{
  "name": "paseo-k8s",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "check:boundary": "node scripts/check-boundary.mjs",
    "check": "npm run typecheck && npm run check:boundary"
  },
  "devDependencies": {
    "@getpaseo/client": "^0.4.0",
    "@tanstack/react-query": "^5.90.11",
    "@types/node": "^24.13.3",
    "@types/react": "~19.2.0",
    "esbuild": "^0.25.12",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "typescript": "^5.9.3",
    "zod": "^4.4.3"
  },
  "description": "Kubernetes workloads, logs, GitOps and a kubectl command bar, as a Paseo plugin",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/jeroenfrenken/paseo-k8s.git"
  }
}
